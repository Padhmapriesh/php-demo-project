# Copy this file to .env (for local docker run --env-file usage)
# In production, set these as real env vars or via EC2/CodeDeploy, never commit real secrets.

DB_HOST=your-rds-endpoint.xxxxxxxxxx.us-east-1.rds.amazonaws.com
DB_NAME=appdb
DB_USER=admin
DB_PASS=your-strong-password
DB_PORT=3306
